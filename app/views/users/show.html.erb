 <div class="mypage" >
    <p><%= @nickname %>さんの投稿一覧</p>
    <% @comments.each do |comment| %>
      <div class="content_post" style="background-image: url(<%= tweet.image %>);">
        <span class="name"><%= comment.ls_name %></span>
      </div>
    <% end %>
  </div>
